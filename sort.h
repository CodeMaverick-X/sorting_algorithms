#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>


/* prototype for test functions used by checker */
void print_array(const int *array, size_t size);


/* prototypes for our functions */
void bubble_sort(int *array, size_t size);

#endif
